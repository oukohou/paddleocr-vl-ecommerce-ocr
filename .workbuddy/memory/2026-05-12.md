# 2026-05-12 工作日志

## PaddleOCR 全球衍生模型挑战赛 - 参赛冲刺

### 完成事项

1. **合成数据生成与效果验证**
   - 在本地Windows环境生成500张合成电商详情页OCR图片
   - 数据集自动划分为：train 400张 / val 50张 / test 50张
   - 标注格式为标准JSONL，兼容PaddleOCR-VL微调
   - 查看样例图片效果，确认版式多样性（多列、艺术字、促销横幅等）满足训练需求

2. **服务器操作手册**
   - 编写 `docs/server_guide.md`，覆盖从SSH登录到训练启动的全流程
   - 包含环境检查、PaddlePaddle/PaddleFormers安装、代码上传、数据生成、训练启动、监控、评估
   - 提供常见问题排查（模型下载慢、OOM、训练中断恢复等）

### 数据分布

| 数据集 | 数量 | 路径 |
|:---|:---|:---|
| 训练集 | 400 | `./data/synthetic/train.jsonl` |
| 验证集 | 50 | `./data/synthetic/val.jsonl` |
| 测试集 | 50 | `./data/synthetic/test.jsonl` |

3. **服务器一键部署脚本**
   - 编写 `setup_server.sh`，在服务器上自动完成：环境检查→虚拟环境→安装PaddlePaddle/PaddleFormers→生成数据→启动训练→自动评估
   - 编写 `push_to_github.sh`，一键推送到GitHub
   - 项目打包为 `paddleocr-vl-project.zip`（53MB，含500张合成图+全部代码）

4. **无脑操作指南**
   - 编写 `docs/QUICKSTART.md`，用户只需3步：传包→执行脚本→等结果

### 项目交付物汇总

| 文件 | 说明 |
|:---|:---|
| `paddleocr-vl-project.zip` | 完整项目包（含合成数据），直接传到服务器 |
| `setup_server.sh` | 服务器上一键执行，全自动完成环境+训练+评估 |
| `configs/ecommerce_ocr_lora.yaml` | LoRA微调配置 |
| `configs/ecommerce_ocr_full.yaml` | 全参微调配置 |
| `scripts/` | 数据生成、标注、训练、推理、评估全套脚本 |
| `docs/QUICKSTART.md` | 3步无脑操作指南 |
| `docs/server_guide.md` | 详细服务器操作手册（含问题排查） |
| `docs/registration.md` | GitHub Issue报名文案+邮件模板 |

### 下一步

- 用户把 `paddleocr-vl-project.zip` 传到服务器，执行 `bash setup_server.sh`
- 训练完成后反馈评估结果
