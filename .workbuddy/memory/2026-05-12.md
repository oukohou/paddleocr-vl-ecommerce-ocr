# 2026-05-12 工作日志

## PaddleOCR 全球衍生模型挑战赛 - 参赛冲刺

### 完成事项

1. **合成数据生成与效果验证**
   - 在本地Windows环境生成500张合成电商详情页OCR图片
   - 数据集自动划分为：train 400张 / val 50张 / test 50张
   - 标注格式为标准JSONL，兼容PaddleOCR-VL微调
   - 查看样例图片效果，确认版式多样性（多列、艺术字、促销横幅等）满足训练需求

2. **服务器操作手册**
   - 编写 `docs/server_guide.md`，覆盖从SSH登录到训练启动的全流程
   - 包含环境检查、PaddlePaddle/PaddleFormers安装、代码上传、数据生成、训练启动、监控、评估
   - 提供常见问题排查（模型下载慢、OOM、训练中断恢复等）

### 数据分布

| 数据集 | 数量 | 路径 |
|:---|:---|:---|
| 训练集 | 400 | `./data/synthetic/train.jsonl` |
| 验证集 | 50 | `./data/synthetic/val.jsonl` |
| 测试集 | 50 | `./data/synthetic/test.jsonl` |

3. **服务器一键部署脚本**
   - 编写 `setup_server.sh`，在服务器上自动完成：环境检查→虚拟环境→安装PaddlePaddle/PaddleFormers→生成数据→启动训练→自动评估
   - 编写 `push_to_github.sh`，一键推送到GitHub
   - 项目打包为 `paddleocr-vl-project.zip`（53MB，含500张合成图+全部代码）

4. **无脑操作指南**
   - 编写 `docs/QUICKSTART.md`，用户只需3步：传包→执行脚本→等结果

### 项目交付物汇总

| 文件 | 说明 |
|:---|:---|
| `paddleocr-vl-project.zip` | 完整项目包（含合成数据），直接传到服务器 |
| `setup_server.sh` | 服务器上一键执行，全自动完成环境+训练+评估 |
| `configs/ecommerce_ocr_lora.yaml` | LoRA微调配置 |
| `configs/ecommerce_ocr_full.yaml` | 全参微调配置 |
| `scripts/` | 数据生成、标注、训练、推理、评估全套脚本 |
| `docs/QUICKSTART.md` | 3步无脑操作指南 |
| `docs/server_guide.md` | 详细服务器操作手册（含问题排查） |
| `docs/registration.md` | GitHub Issue报名文案+邮件模板 |

5. **GitHub 仓库创建与代码推送**
   - 使用 GitHub API 创建公开仓库 `paddleocr-vl-ecommerce-ocr`
   - 推送本地 30 个文件（代码、配置、文档、脚本）到 main 分支
   - 仓库地址：https://github.com/oukohou/paddleocr-vl-ecommerce-ocr
   - 已移除嵌套子仓库 `paddleformers-guide/`，避免 clone 异常

6. **混合AI+PIL数据集生成（核心改进）**
   - 策略：AI生成高质量电商背景（13张） + PIL叠加精确中文文字（100%标注准确）
   - 生成13张AI背景：红/橙/白/灰/紫等多种电商场景风格
   - 使用 `scripts/generate_hybrid_quick.py` 生成 30 train / 5 val / 5 test
   - 自动避开AI水印区域（右下角130x50px不放置文字）
   - 文字多样性：标题、参数、促销、按钮、长段落，多种字体大小和颜色
   - 效果验证：背景质感远超PIL纯色，文字标注完全精确

### 数据集汇总

| 数据集 | 类型 | 数量 | 路径 |
|:---|:---|:---|:---|
| 纯PIL合成 | train/val/test | 400/50/50 | `./data/synthetic/` |
| AI+PIL混合 | train/val/test | 30/5/5 | `./data/hybrid/` |
| AI背景素材 | 背景图 | 13 | `./data/hybrid/ai_backgrounds/` |

### 追加工作（23:40）

7. **扩充混合数据集**
   - 编写 `scripts/expand_hybrid_data.py`（追加模式，不覆盖已有数据）
   - 基于现有10张AI背景，通过不同文字布局随机叠加
   - 新增：train 70 / val 15 / test 15
   - **最终规模：train 100 / val 20 / test 20，共140张图片**
   - 已 commit（432c1c7），push因环境credential问题需手动完成

8. **服务器环境配置与训练指南**
   - 编写 `docs/server_setup.md`，覆盖完整服务器部署流程
   - 包含：conda独立环境创建、PaddlePaddle/PaddleFormers安装、代码准备、数据准备
   - 提供LoRA训练配置（`configs/ecommerce_ocr_lora.yaml` 参数详解）
   - 包含：启动命令、单卡调试、后台运行（tmux/nohup）、VisualDL监控
   - 包含：LoRA权重合并、测试集评估脚本
   - **重点强调：一人一conda环境，显存隔离，日志隔离，不污染他人环境**
   - 常见问题排查：命令找不到、OOM、模型下载慢、中文乱码
   - 提供一键启动脚本 `scripts/start_train.sh`

### 追加工作（5月13日 11:00）

9. **系统整理服务器训练完整流程**
   - 重写 `docs/SERVER_GUIDE.md`，从零到训练评估的端到端指南
   - 更新 `configs/ecommerce_ocr_lora.yaml` 和 `configs/ecommerce_ocr_full.yaml`：
     - 数据路径从 `./data/synthetic/` 改为 `./data/hybrid/`
     - `max_seq_len` 从 16384 降至 4096（合成数据文本短，省显存）
     - 控制方式从 `num_train_epochs=3` 改为 `max_steps=100`（100张小数据集约100个epoch）
     - `eval_steps=10` / `save_steps=20`（小数据集频繁验证保存）
     - 开启 `disable_tqdm=false`（方便看进度）
   - 更新 `scripts/train.sh` 指向 hybrid 数据集
   - GitHub 推送完成（aa2a71f）

### 服务器训练速查（5步）

```bash
# 1. 创建环境
conda create -n paddleocr-vl python=3.10 -y && conda activate paddleocr-vl

# 2. 安装依赖
pip install paddlepaddle-gpu==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu123/
pip install paddleformers Pillow numpy tqdm Levenshtein visualdl

# 3. 获取代码
git clone https://github.com/oukohou/paddleocr-vl-ecommerce-ocr.git
cd paddleocr-vl-ecommerce-ocr

# 4. 训练（一键）
bash scripts/train.sh lora

# 5. 评估
python -m paddle.distributed.launch --log_dir ./log \
    scripts/eval.py \
    --model_name_or_path ./outputs/ecommerce-ocr-lora/export \
    --data_path ./data/hybrid/test.jsonl \
    --output_path ./eval_results.jsonl
```

### 下一步

- 用户把项目传到服务器，按 `docs/SERVER_GUIDE.md` 步骤操作
- 先单卡跑1个epoch验证数据管道通不通
- 再切8卡正式训练
- 训练完成后反馈评估结果
